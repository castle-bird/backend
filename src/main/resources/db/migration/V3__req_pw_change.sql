--------------------------------------------------------------------------------
-- 최초 로그인시 비번 초기화 강제를 위함
-- DEFAULT false 이유 →
-- 1. 기존 사용자들: 마이그레이션 시 전원 강제 변경 방지
-- 2. 최초 관리자 계정: DataInitializer로 생성하므로 변경 불필요
-- 신규 일반 직원은 앱 코드에서 true로 세팅
--------------------------------------------------------------------------------
ALTER TABLE employees ADD COLUMN password_change_required BOOLEAN NOT NULL DEFAULT false;